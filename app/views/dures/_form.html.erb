<%= form_with(model: dure, local: true) do |form| %>
  <% if dure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dure.errors.count, "error") %> prohibited this dure from being saved:</h2>

      <ul>
      <% dure.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :activity %>
    <%= form.text_field :activity %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="field">
    <%= form.label :effective %>
    <%= form.number_field :effective %>
  </div>

  <div class="field">
    <%= form.label :other %>
    <%= form.text_area :other %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
